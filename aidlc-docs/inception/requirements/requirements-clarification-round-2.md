# 요구사항 추가 확인 질문 2차

어려운 운영 용어를 포트폴리오 프로젝트 기준으로 다시 설명했습니다.
각 질문의 `[Answer]:` 뒤에 선택한 문자를 입력하세요. `X`를 선택하면 원하는 내용을 함께 적어주세요.

## 질문 1: 변경 관리
변경 관리는 새 버전을 배포하기 전에 무엇을 바꿨고, 문제가 생기면 어떻게 되돌릴지 기록하는 절차입니다.

A) 기존 회사나 팀의 배포 승인 절차를 사용하고 그 이름을 함께 작성

B) 별도 절차가 없으므로 변경 내용·확인자·되돌리는 방법을 적는 간단한 체크리스트를 생성

C) 포트폴리오이므로 공식 승인 절차는 생략하고 Git 변경 이력만 관리

X) 기타 (아래 `[Answer]:` 뒤에 설명)

[Answer]:C

## 질문 2: 배포 실패 시 되돌리는 방법
새 버전에 문제가 생겼을 때 정상 동작하던 이전 버전으로 돌아가는 방법입니다.

A) 이전 Git 태그와 배포 파일을 다시 배포 — 가장 단순하고 포트폴리오에 적합

B) 신버전과 구버전을 동시에 실행한 뒤 트래픽을 구버전으로 전환 — 빠르지만 비용 증가

C) 일부 사용자에게만 신버전을 배포하고 오류가 증가하면 자동으로 구버전 복귀 — 안전하지만 설정이 복잡

D) 코드뿐 아니라 데이터베이스 구조 변경도 자동으로 이전 상태로 복구 — 가장 복잡

X) 기타 (아래 `[Answer]:` 뒤에 설명)

[Answer]:A

## 질문 3: 새 버전 배포 방식
사용 중인 서비스를 새 버전으로 교체하는 방식입니다.

A) 기존 버전 위에 바로 배포 — 가장 저렴하고 단순하지만 짧은 장애 가능

B) 실행 서버를 하나씩 새 버전으로 교체 — 서비스 중단 위험이 낮지만 컨테이너 환경이 필요할 수 있음

C) 구버전과 신버전을 동시에 준비한 후 한 번에 전환 — 중단 위험은 낮지만 일시적으로 비용이 약 두 배

D) 소수 트래픽부터 신버전으로 보내고 문제가 없으면 점차 확대 — 안전하지만 관측과 자동화 설정이 복잡

X) 기타 (아래 `[Answer]:` 뒤에 설명)

[Answer]:A

## 질문 4: AWS 리전 구성
AWS 리전은 서울처럼 데이터센터가 모인 지역입니다. 여러 리전을 쓰면 서울 리전 전체 장애에도 대응할 수 있지만 비용과 복잡도가 크게 증가합니다.

A) 서울 리전 하나를 사용하되 리전 안의 여러 가용영역을 사용 — 일반적인 포트폴리오에 적합

B) 서울 리전 장애 시 다른 리전에서 서비스를 켤 수 있도록 대기 환경 구성 — 비용과 운영 난이도 증가

C) 두 리전에서 서비스를 항상 동시에 실행 — 가장 안전하지만 비용과 구현 난이도가 매우 높음

X) 기타 (아래 `[Answer]:` 뒤에 설명)

[Answer]:A

## 질문 5: 장애 대응 절차
서비스 오류가 발생했을 때 누가 확인하고, 어떻게 복구하고, 같은 오류를 예방할지 정하는 절차입니다.

A) 기존 회사나 팀의 장애 대응 절차를 사용하고 그 이름을 함께 작성

B) 별도 절차가 없으므로 알림 확인·원인 조사·복구·사후 기록으로 구성된 간단한 절차를 생성

X) 기타 (아래 `[Answer]:` 뒤에 설명)

[Answer]:대학생 팀플이기에 이런건없음
